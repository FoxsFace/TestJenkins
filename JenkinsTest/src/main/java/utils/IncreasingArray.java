/*package utils;

import Models.PostUser;

public class IncreasingArray {

    private static boolean isSorted = true;

    public static boolean arrayIsSorted(PostUser[] users){

        for (int i = 0; i < users.length - 1; i++) {
            if (users[i].getId() > users[i+1].getId()) {
                isSorted = false;
                break;
            }
        }
        return isSorted;
    }
}
*/